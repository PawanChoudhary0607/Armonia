// lib/widgets/home/curated_playlist_carousel.dart
//
// Phase 5B — Curated playlists removed.
// This file is kept as an empty widget so no import elsewhere breaks.

import 'package:flutter/material.dart';

/// Placeholder — curated playlists have been removed in Phase 5B.
/// This widget renders nothing and is kept only for import compatibility.
class CuratedPlaylistCarousel extends StatelessWidget {
  const CuratedPlaylistCarousel({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
